

<% layout('./layouts/boilerplates') %>
<div id="cluster-map"></div>

<main class="container">
    <body>
        <!-- <div class="container"></div> -->
        <h2 style="text-align: center;margin: 50px;">All campgrounds</h2>
        <!-- <h1 class="n">All Campgrounds </h1> -->
        <!-- <h2>
        <figure class="text-center m-5">
            <blockquote class="blockquote">
              <p>All Campground </p>    

            </blockquote>
            figcaption class="blockquote-footer">
             Choose your favourite one
            </figcaption> -->
          <!-- </figure> -->
        <!-- </h2>--> 

        <% for(let campground of campgrounds){ %>
            <div class="card mb-3 " >
                <div class="row">
                    <div class="col-md-4">
                        <% if (campground.images.length){ %>
                        <img crossorigin="anonymous" class="img-fluid" alt="" src="<%= campground.images[0].url %>">
                        <% }else{ %>
                            <img   crossorigin="anonymous" class="img-fluid"  src="https://res.cloudinary.com/dnsw1mkyu/image/upload/v1693089639/YelpCamp/oytkedqylq3ghygvoowb.jpg" alt="">
                            <% } %>
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title"><%= campground.title %></h5>
                            <p class="card-text"><%= campground.description %></p>
                            <p class="card-text">
                                <small class="text-muted">
                                    <%= campground.location %>
                                </small>
                            </p>
                            <a href="/campgrounds/<%= campground._id  %>" class="btn btn-primary"> View <%= campground.title  %></a>
                        </div>
                    </div>
                </div>
            </div>
            <% } %>
            <div>
                <a  class="btn btn-primary mb-3" href="/campgrounds/new">Add Camground</a>
            </div>
        </div>
        </body>
    </main>

    <script>
        const mapToken = '<%-process.env.MAPBOX_TOKEN%>';
        //We adding s to campground to attatch the whole campgrounds
        const campgrounds  = {features:<%-JSON.stringify(campgrounds)%>} 
        
    </script>
    <script src="/javascripts/clusterMap.js"></script>
    </html>

